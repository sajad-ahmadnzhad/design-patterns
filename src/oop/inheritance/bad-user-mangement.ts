class User {
    login() {
        //Logic for login user
    }

    register() {
        //Logic for register user
    }

    refreshToken() {
        //Logic for refresh token
    }
}

const user = new User()

user.login()
user.refreshToken()
user.register()


class Admin {
    login() {
        //Logic for login admin
    }

    register() {
        //Logic for register admin
    }

    refreshToken() {
        //Logic for refresh token
    }
}

const admin = new Admin()

admin.login()
admin.refreshToken()
admin.register()